// components/AdBanner.jsx
import React from 'react';

const AdBanner = () => {
  return (
    <div style={{
      background: '#fff',
      padding: '1.2rem',
      border: '1px dashed #aaa',
      borderRadius: '8px',
      textAlign: 'center',
      color: '#777',
      marginBottom: '2rem',
    }}>
      🔔 Reklam Alanı (Google AdSense kodları buraya entegre edilecek)
    </div>
  );
};

export default AdBanner;
